# Session Notes — 2026-05-11

## Project
Kaggle — NVIDIA Nemotron Model Reasoning Challenge

## Session objective
Start the 2026-05-11 Nemotron session from the latest repo/session-note state, analyze the newly confirmed 0.54 public-score submission, push updated Notebook B/C variants, debug Kaggle runtime failures, complete the assistant-only loss training run, package the adapter, and submit the new candidate for scoring.

## Starting state
- Previous confirmed best before today: **0.53** from Notebook C Version 11 / SFT64 path.
- Demo/baseline level: **0.50**.
- `v2_bit_512` had scored **0.49**, below baseline and not selected.
- `adapter_sft_v2_bit_bal128` had been submitted previously, with score awaited in the 2026-05-07 notes.

## New score confirmation
User confirmed from Kaggle submissions page:

| Submission | Public score | Adapter tag | Interpretation |
|---|---:|---|---|
| `notebook_c_adapter_validation_submission_v11_harde - Version 2` | **0.54** | `adapter_sft_v2_bit_bal128` | New best; controlled bit-heavy blend helped |
| `notebook_c_adapter_validation_submission_v11_harde - Version 3` | 0.51 | not used for final candidate | Regression; do not select |
| earlier `v2_bit_512` | 0.49 | `adapter_sft_v2_bit_512` | Too broad / harmful bit mix |

Current best became:

```text
adapter_sft_v2_bit_bal128 — public score 0.54
```

Action recommended:
- Keep **Version 2 / 0.54** selected as current final candidate.
- Do not select Version 3 or `v2_bit_512`.

## What worked in the 0.54 run
The Version 2 attachment/log confirmed:

```python
EXPECTED_ADAPTER_TAG = "adapter_sft_v2_bit_bal128"
```

Notebook C packaging validation for that run:
- selected source zip: `adapter_sft_v2_bit_bal128.zip`
- `adapter_config.json` present
- `adapter_model.safetensors` present
- LoRA rank `r = 32`
- files were at zip root
- `submission.zip` package size about 2963 MB

Interpretation:
- The 0.54 improvement was from a **controlled bit-heavy 128-example blend**, not from simply scaling.
- The best-known sample plan was:

```python
SAMPLE_PLAN = {
    "bit_manipulation": 65,
    "gravity": 21,
    "unit_conversion": 21,
    "numeral": 21,
}
```

## Main hypothesis after score review
The model benefits from small, clean, controlled SFT, but degrades when too much compact/new bit data is added.

Working hypothesis:
1. Bit data is useful.
2. Compact bit traces can hurt when scaled too aggressively.
3. Full-text loss is probably inefficient because it trains on user/prompt tokens as well as assistant solution tokens.
4. Next high-ROI experiment should keep the winning 0.54 data mix fixed and change only the loss mask to assistant-only loss.

## Git work completed today

### 1. Added Notebook B assistant-only loss candidate
File pushed:

```text
notebooks/notebook_b_sft_assistant_loss_bal128_20260511.ipynb
```

Commit:

```text
c22bcf9f90b751a1d729e73754b55703bf40ed58
```

Purpose:
- Repeat the 0.54 `adapter_sft_v2_bit_bal128` sample mix.
- Train with assistant-only labels instead of full-text labels.
- Output tag:

```python
OUTPUT_TAG = "adapter_sft_v2_bit_bal128_asstloss"
```

### 2. Added Notebook C v12 packaging candidate
File pushed:

```text
notebooks/notebook_c_adapter_validation_submission_v12_20260511.ipynb
```

Commit:

```text
756fce3e96d02183193bc2f863d333af3d20c617
```

Purpose:
- Package any Notebook B adapter by exact tag.
- Default expected tag:

```python
EXPECTED_ADAPTER_TAG = "adapter_sft_v2_bit_bal128_asstloss"
```

### 3. Added CUTLASS-path patched Notebook B
Initial Notebook B run failed before model load with:

```text
ModuleNotFoundError: No module named 'cutlass'
```

Cause:
- Kaggle exposed the utility path as:

```text
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia_utility_script
```

- The first pushed Notebook B checked the old hyphenated path:

```text
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia-utility-script
```

File pushed:

```text
notebooks/notebook_b_sft_assistant_loss_bal128_20260511_cutlassfix.ipynb
```

Commit:

```text
af80c09f8e314f5fe22b9267767b9db519d18bf2
```

Fix:
- Search both utility roots.
- Add `nvidia_cutlass_dsl/python_packages` to `sys.path`.
- Verify `import cutlass` before model load.

### 4. Added target-module regex patched Notebook B
After CUTLASS fix, model load succeeded but PEFT injection failed:

```text
ValueError: Target modules .*\\.(in_proj|out_proj|up_proj|down_proj)$ not found in the base model.
```

Cause:
- Regex was over-escaped in the notebook and matched a literal backslash before the dot.

File pushed:

```text
notebooks/notebook_b_sft_assistant_loss_bal128_20260511_targetfix.ipynb
```

Commit:

```text
1181e7caa5f3c772efe12f3a30f9bb9c1af8a19c
```

Fix:

```python
TARGET_MODULES_REGEX = r'.*\.(in_proj|out_proj|up_proj|down_proj)$'
```

Added validation before PEFT injection:

```text
Matched LoRA target modules: <number>
First 20 matched targets:
...
```

## Kaggle Notebook B final successful run
User ran:

```text
notebook_b_sft_assistant_loss_bal128_20260511_targetfix.ipynb
```

### Runtime/setup results
- CUTLASS import: **PASS**
- ptxas prepared: **PASS**
- `ptxas-blackwell` prepared: **PASS**
- Triton ptxas version check patched: **PASS**
- Trace file found:

```text
/kaggle/input/notebooks/jatalepawan/nemotron-trace-generator-v2-bit-20260506/train_traces_v2_bit.jsonl
```

### Trace corpus loaded
Total trace records: **5635**

Counts:
- gravity: 1597
- unit_conversion: 1594
- numeral: 1576
- bit_manipulation: 868

### Sample selected
Selected records: **128**

Counts:
- bit_manipulation: 65
- unit_conversion: 21
- gravity: 21
- numeral: 21

Validation:

```text
Sampling validation passed.
```

### Model / LoRA setup
- Model path:

```text
/kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1
```

- Base model loaded successfully.
- Fast path patched:

```text
Patched fast path modules: ['transformers_modules._1.modeling_nemotron_h']
```

- LoRA target modules matched:

```text
Matched LoRA target modules: 5980
```

First targets included:
- `backbone.layers.0.mixer.in_proj`
- `backbone.layers.0.mixer.out_proj`
- MoE expert `up_proj` / `down_proj` modules

Trainable params:

```text
trainable params: 880,138,240 || all params: 32,458,075,584 || trainable%: 2.7116
```

### Assistant-only encoding
- Encoded examples: **128**
- Skipped: **0**
- Label modes:

```text
Counter({'assistant_only': 128})
```

Example:
- first encoded length: 384
- category: bit_manipulation
- train-label tokens: 125

### Training result
Training completed successfully.

Key logs:

```text
epoch=1 item=1/128 step=1 loss=1.5079
epoch=1 item=10/128 step=10 loss=1.5006
epoch=1 item=20/128 step=20 loss=0.0904
epoch=1 item=100/128 step=100 loss=0.0012
epoch=1 item=128/128 step=128 loss=0.0799
```

Final metrics:

```text
Mean loss: 0.22671877459106327
Last 10 mean loss: 0.08472448063548654
```

Interpretation:
- This is the cleanest Notebook B run so far.
- Loss improved cleanly under assistant-only labels.
- However, public score must decide whether this improves leaderboard accuracy.

### Adapter output
Output tag:

```text
adapter_sft_v2_bit_bal128_asstloss
```

Saved files:
- `README.md`
- `adapter_config.json`
- `adapter_model.safetensors`
- tokenizer debug files

Debug zip:

```text
/kaggle/working/adapter_sft_v2_bit_bal128_asstloss.zip
```

Zip size:

```text
2983.93 MB
```

Zip contents:
- `adapter_config.json`
- `adapter_model.safetensors`

Notebook B result: **PASS**.

## Kaggle Notebook C packaging run
User ran Notebook C v12 after saving/running Notebook B output.

Important runtime context:
- There was no RTX 6000 option available for Notebook C.
- This was acceptable because `RUN_VLLM_VALIDATION = False` and Notebook C only packages the adapter.
- There was no direct “submit from notebook” option; user used Save & Run and then submitted from the competition page.

Notebook C config:

```python
EXPECTED_ADAPTER_TAG = "adapter_sft_v2_bit_bal128_asstloss"
```

### Adapter discovery
Notebook C found:

```text
/kaggle/input/notebooks/jatalepawan/notebook-b-v12-nemotron-sft-with-assistant-only/adapter_sft_v2_bit_bal128_asstloss/adapter_config.json
/kaggle/input/notebooks/jatalepawan/notebook-b-v12-nemotron-sft-with-assistant-only/adapter_sft_v2_bit_bal128_asstloss/adapter_model.safetensors
/kaggle/input/notebooks/jatalepawan/notebook-b-v12-nemotron-sft-with-assistant-only/adapter_sft_v2_bit_bal128_asstloss.zip
```

Selected source zip:

```text
/kaggle/input/notebooks/jatalepawan/notebook-b-v12-nemotron-sft-with-assistant-only/adapter_sft_v2_bit_bal128_asstloss.zip
```

Selected adapter dir:

```text
/kaggle/working/adapter_for_validation_v12_hardened
```

Adapter files:
- `adapter_config.json`
- `adapter_model.safetensors` (~3359.18 MB extracted)

### Adapter validation
Rank:

```text
r = 32
```

Target modules:

```text
.*\.(in_proj|out_proj|up_proj|down_proj)$
```

Validation:

```text
Adapter structure validation passed.
```

### Final submission package
Created:

```text
/kaggle/working/submission.zip
```

Size:

```text
2988.55 MB
```

Zip contents:
- `adapter_config.json`
- `adapter_model.safetensors`

Validation:

```text
Minimal submission.zip packaging validation passed.
```

Notebook C result: **PASS**.

## Submission status
User submitted from Kaggle competition page:

```text
Notebook C-Adapter Validation & Submission Pack Tr - Version 12
Status: Pending
```

Suggested description used/planned:

```text
asstloss_bal128: 0.54 bal128 mix + assistant-only loss, rank32
```

Status at session close:
- submitted: **yes**
- score: **pending**
- current selected best should remain: **Version 2 / 0.54** until Version 12 scores

## Current leaderboard candidate state

| Candidate | Adapter tag | Public score / status | Action |
|---|---|---:|---|
| Current best | `adapter_sft_v2_bit_bal128` | **0.54** | Keep selected |
| New candidate | `adapter_sft_v2_bit_bal128_asstloss` | Pending | Evaluate after score arrives |
| Old bit scale | `adapter_sft_v2_bit_512` | 0.49 | Do not select |
| Later regression | unknown / Version 3 | 0.51 | Do not select |

## Interpretation guide for Version 12 result

| Public score | Interpretation | Next action |
|---:|---|---|
| > 0.54 | Assistant-only loss helped | Keep as new best; consider same approach with one small trace improvement |
| 0.54 | Loss masking preserves gain but no lift | Keep old selected candidate; inspect category mix / vLLM behavior |
| 0.50–0.53 | Assistant-only loss hurt generation or trace style | Revert to full-text loss for bal128; improve traces before retry |
| < 0.50 | Serious training objective mismatch | Do not select; debug outputs before further assistant-loss runs |
| error | Unexpected packaging/eval issue | Debug vLLM adapter load / rank / zip root first |

## Current hypotheses after today
1. **Controlled bit-heavy sample is beneficial.**
   - `adapter_sft_v2_bit_bal128` scored 0.54.

2. **Training mechanics are now reliable.**
   - CUTLASS path fixed.
   - Target-module regex fixed.
   - Model loads.
   - LoRA injects.
   - Training completes.
   - Packaging passes.

3. **Assistant-only loss is now a valid controlled experiment.**
   - It trained cleanly with lower loss.
   - Public score pending.

4. **Notebook C does not need RTX 6000 unless vLLM validation is enabled.**
   - Packaging-only path works on non-RTX6000 runtime.

5. **Linear KAG-1 is stale.**
   - It still says tiny LoRA training is blocked, but Kaggle-native LoRA training and packaging now work.

## Recommended next-session plan
1. Check Version 12 public score.
2. If Version 12 > 0.54:
   - select Version 12 as new best.
   - run one conservative next experiment: assistant-only loss + improved bit trace verbosity or small cipher addition.
3. If Version 12 <= 0.54:
   - keep Version 2 selected.
   - do not scale assistant-only loss immediately.
   - inspect whether assistant-only masking removed useful format imitation from full-text training.
4. Update Linear KAG-1 to reflect resolved training blocker and create/track the new optimization issue if needed.
5. Preserve all successful notebooks and tags:
   - `adapter_sft_v2_bit_bal128` = current best 0.54
   - `adapter_sft_v2_bit_bal128_asstloss` = pending controlled experiment

## Experiment table

| Priority | Experiment | Hypothesis | Success criterion | Risk |
|---|---|---|---|---|
| P0 | Wait for Version 12 score | Assistant-only loss improves bal128 | Public score > 0.54 | Could hurt format imitation |
| P1 | If win: small bit-serial trace v3 | More explicit bit reasoning improves bit category | Score lift above new best | Longer traces / truncation |
| P2 | If no win: compare full-text vs assistant-only | Full-text may teach formatting better | Identify why 0.54 worked | Requires local generation debug |
| P3 | Add optional vLLM smoke validation in C | Catch bad boxed outputs pre-submit | 3–10 examples produce boxed answers | Runtime/GPU availability |
| P4 | Update Linear KAG-1 | Keep project tracker accurate | KAG-1 no longer stale | Admin overhead |

## Closing state
- Current best public score: **0.54** from `adapter_sft_v2_bit_bal128`.
- New assistant-only-loss adapter trained: **yes**.
- New assistant-only-loss adapter packaged: **yes**.
- New submission made: **yes**.
- New submission status: **pending**.
- Keep Version 2 selected until Version 12 score returns.
