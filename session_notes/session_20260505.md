# Session Notes — 2026-05-05

## Project
Kaggle — NVIDIA Nemotron Model Reasoning Challenge

## Session objective
Continue from the 2026-05-04 package-validity test and move one step toward a score-improving LoRA submission by scaling the Notebook B smoke SFT run from 6 examples to ~64 examples, then packaging through Notebook C.

## Starting state
- Previous Notebook C package-validity submission scored **0.50**, matching the official NVIDIA Nemotron Submission Demo baseline.
- This confirmed that our custom `submission.zip` format, `adapter_config.json`, rank-32 LoRA adapter, and Kaggle evaluator/vLLM loading path were valid.
- The next goal was to test whether a slightly larger deterministic trace SFT adapter could still package and submit successfully.

## Important competition constraints remembered
- Final submission must be a `submission.zip` containing a compatible LoRA adapter.
- `adapter_config.json` must be present.
- LoRA rank must be at most 32.
- The competition metric loads the Nemotron-3-Nano-30B base model with the submitted adapter using vLLM.
- The evaluator prioritizes answers inside `\boxed{}`.

## Notebook B — SFT smoke run update
Notebook B used the v1.1 trace corpus generated by Notebook A.

### Config used
- `TRACE_VERSION = "v1_1"`
- `SMOKE_SAMPLE_SIZE = 64`
- Actual sampled records: **63**
- Balanced category sample:
  - `gravity`: 21
  - `unit_conversion`: 21
  - `numeral`: 21
- `MAX_SEQ_LEN = 384`
- `LR = 2e-4`
- `LORA_RANK = 32`
- `LORA_ALPHA = 64`
- `LORA_DROPOUT = 0.05`
- Output directory changed to:
  - `/kaggle/working/adapter_sft64_v1_1`

### Training result
- Model loaded on NVIDIA RTX PRO 6000 Blackwell Server Edition.
- LoRA attached to target regex:
  - `.*\.(in_proj|out_proj|up_proj|down_proj)$`
- Trainable parameters shown:
  - `880,138,240`
  - `2.7116%` of total params
- Manual SFT loop completed all 63 steps.
- Loss declined from roughly `4.88` at step 1 to the `0.2–0.5` range by the later steps.
- Mean loss:
  - `0.7705303335946704`

### Adapter artifacts saved
Notebook B saved:
- `/kaggle/working/adapter_sft64_v1_1/adapter_config.json`
- `/kaggle/working/adapter_sft64_v1_1/adapter_model.safetensors`
- `/kaggle/working/adapter_sft64_v1_1/tokenizer_debug/...`

The debug zip cell was corrected from:

```python
SMOKE_ZIP_PATH = WORKING_DIR / "adapter_smoke_v1_1.zip"
```

to:

```python
SMOKE_ZIP_PATH = WORKING_DIR / "adapter_sft64_v1_1.zip"
```

Notebook B produced:
- `/kaggle/working/adapter_sft64_v1_1.zip`
- zip contents:
  - `adapter_config.json`
  - `adapter_model.safetensors`
- zip size reported in Notebook B:
  - about `2836.54 MB`

## Notebook C — packaging update
Notebook C was updated to search for:
- `adapter_sft64_v1_1`
- `adapter_sft64_v1_1.zip`
- old `adapter_smoke_v1_1` fallback

`RUN_VLLM_VALIDATION` was set to `False`, because prior local Notebook C vLLM validation failed due to missing `vllm`, while Kaggle evaluator had already proven that the adapter-loading path works.

### Early issue encountered
Notebook C initially did **not** show the new Notebook B `adapter_sft64_v1_1` output under `/kaggle/input`.

Earlier diagnostic output showed only:
- `/kaggle/working/adapter_for_validation_v1_1/adapter_config.json`
- `/kaggle/working/adapter_for_validation_v1_1/adapter_model.safetensors`
- `/kaggle/working/submission.zip`

Notebook C initially extracted:
- `/kaggle/working/submission.zip`

and selected:
- `/kaggle/working/adapter_for_validation_v1_1`

This created temporary provenance ambiguity.

### Final Notebook C output before close — provenance corrected
At session close, the final Notebook C output showed the expected Notebook B SFT64 artifacts mounted under `/kaggle/input`:

- `/kaggle/input/notebooks/jatalepawan/nb-b-nemotron-sft-smoke-run-v1-1-trace-corpus/adapter_sft64_v1_1/adapter_config.json`
- `/kaggle/input/notebooks/jatalepawan/nb-b-nemotron-sft-smoke-run-v1-1-trace-corpus/adapter_sft64_v1_1/adapter_model.safetensors`
- `/kaggle/input/notebooks/jatalepawan/nb-b-nemotron-sft-smoke-run-v1-1-trace-corpus/adapter_sft64_v1_1.zip`

Notebook C then printed:

```text
Extracted adapter zip candidate: /kaggle/input/notebooks/jatalepawan/nb-b-nemotron-sft-smoke-run-v1-1-trace-corpus/adapter_sft64_v1_1.zip
```

It selected:

```text
Selected ADAPTER_DIR: /kaggle/working/adapter_for_validation_v1_1
```

This is acceptable because `/kaggle/working/adapter_for_validation_v1_1` was freshly extracted from the correct `adapter_sft64_v1_1.zip`.

Selected adapter file:
- `adapter_model.safetensors`: about `3359.18 MB`

### Final packaging result
Notebook C created a valid root-level `submission.zip`:
- `/kaggle/working/submission.zip`
- size: about `2810.91 MB`
- zip contents:
  - `adapter_config.json`
  - `adapter_model.safetensors`

Notebook C validations passed:
- `adapter_config.json` present
- adapter weights present
- LoRA rank detected as `32`
- rank <= competition max rank 32
- zip files at root, not nested

## Submission status
The Notebook C package was submitted to Kaggle for scoring.

Status at session close:
- **Submitted:** yes
- **Score:** awaited

Updated interpretation:
- The final Notebook C output indicates that the submitted artifact was very likely the intended **SFT64 adapter**, because Notebook C visibly mounted and extracted `adapter_sft64_v1_1.zip` from the Notebook B output.
- This is now cleaner than the earlier ambiguous packaging attempt.
- Still, this is only a 63-example training run, so it should be treated as a scale/provenance test, not a competitive score attempt.

## Interpretation of possible results

### If score is 0.50
Likely interpretations:
1. 63 examples are too small to move public score.
2. The three covered categories are not enough for leaderboard movement.
3. Full-text loss training may be inefficient versus assistant-only loss.

Action:
- Treat as successful pipeline/provenance test.
- Move to SFT128/SFT512 and/or assistant-only loss.

### If score is above 0.50
Likely interpretation:
- SFT64 is already nudging behavior in useful directions.

Action:
- Preserve this notebook version.
- Scale to 128 or 512 examples with the same packaging path.

### If submission errors
Likely interpretation:
- Adapter file or vLLM compatibility issue despite package validation.

Action:
- Debug `adapter_config.json`, zip root, and vLLM compatibility.

## Main lessons from today
1. The overall training mechanics can scale from 6 examples to 63 examples inside Kaggle.
2. Notebook B now creates a correctly named `adapter_sft64_v1_1.zip`.
3. Quick Save was not enough to expose the new adapter to Notebook C; Save & Run All / adding the correct Notebook B output was required.
4. Final Notebook C output confirmed clean SFT64 provenance by mounting and extracting `adapter_sft64_v1_1.zip` from `/kaggle/input`.
5. Future Notebook C should still hard-fail unless the selected adapter path or extracted zip explicitly contains the expected experiment id, e.g. `adapter_sft64_v1_1`.

## Required Notebook C guardrail for next session
Add a hard provenance check before packaging. Since the selected directory may be a generic extraction directory, track the extracted zip path as well:

```python
EXPECTED_ADAPTER_TAG = "adapter_sft64_v1_1"
selected_path = str(ADAPTER_DIR)
source_zip_path = str(EXTRACTED_ZIP_PATH)  # set this when extracting
if EXPECTED_ADAPTER_TAG not in selected_path and EXPECTED_ADAPTER_TAG not in source_zip_path:
    raise RuntimeError(
        f"Selected adapter/source does not contain {EXPECTED_ADAPTER_TAG}: "
        f"ADAPTER_DIR={selected_path}, EXTRACTED_ZIP_PATH={source_zip_path}. "
        "Do not package ambiguous or stale adapters."
    )
```

Also avoid extracting `/kaggle/working/submission.zip` as a source candidate unless intentionally debugging prior submissions.

## Recommended next actions
1. Wait for Kaggle score from the submitted SFT64 package.
2. Record the score and evaluator status.
3. If it scores successfully, preserve the Notebook B and Notebook C versions as the first clean SFT64 submission path.
4. Patch Notebook C to:
   - delete stale `/kaggle/working/submission.zip` before discovery,
   - delete stale `/kaggle/working/adapter_for_validation_v1_1`,
   - ignore `/kaggle/working/submission.zip` as an input source,
   - require `adapter_sft64_v1_1` in selected path or extracted zip path.
5. If SFT64 completes with 0.50 or better, scale next to 128 or 512 examples.
6. Before full corpus training, add assistant-only loss masking.

## Next experiment plan

| Priority | Experiment | Hypothesis | Success criterion | Risk |
|---|---|---|---|---|
| P0 | Wait for SFT64 score | SFT64 package is evaluator-compatible | Kaggle completes with score | Score may remain 0.50 |
| P1 | Patch Notebook C provenance guard | Prevent stale/ambiguous adapter packaging | Hard fail unless expected source is selected | Requires another Notebook C save/version |
| P2 | SFT128/SFT512 | More examples may move score | Score > 0.50 or at least valid | Overfit/format drift |
| P3 | Assistant-only loss | Better training signal than full-text loss | Lower validation loss / better outputs | Implementation complexity |
| P4 | Add bit/cipher traces | Broader category coverage should improve accuracy | New validated trace corpus + score lift | Trace correctness risk |

## Session close state
- Notebook B SFT64 training: **completed**
- Notebook B SFT64 zip creation: **completed**
- Notebook C final packaging: **completed with clean SFT64 provenance**
- Kaggle submission: **submitted**
- Score: **awaited**
- Next session starts with: check SFT64 score, patch Notebook C provenance guard, then decide SFT128/SFT512 vs assistant-only loss.
