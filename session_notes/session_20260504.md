# Session Notes — 2026-05-04

## Project
Kaggle — NVIDIA Nemotron Model Reasoning Challenge

## Session focus
Continue from 2026-04-30. Validate the smoke LoRA adapter produced by Notebook B, build Notebook C for adapter packaging/validation, and submit a low-stakes smoke adapter to test Kaggle evaluator compatibility.

## Starting state from prior session

Notebook A and Notebook B were already working:

1. **Notebook A — Trace Generator v1.1**
   - File: `notebooks/nemotron_trace_generator_20260429.ipynb`
   - Produced validated corpus: `train_traces_v1_1.jsonl`
   - Records generated: 4,767
   - Categories:
     - gravity: 1,597
     - unit_conversion: 1,594
     - numeral: 1,576
   - Validation passed:
     - literal `\boxed{...}` answers
     - boxed answer equals `train.answer`
     - no Python backspace escape bug
     - no inconsistent stated numeric values

2. **Notebook B — SFT Smoke Run v1.1**
   - File: `notebooks/nemotron_sft_smoke_v1_1_20260430.ipynb`
   - Consumed `train_traces_v1_1.jsonl`
   - Ran tiny manual PEFT/LoRA smoke update with 6 examples
   - Successful training evidence:
     ```text
     step 01/6 | loss=4.6033
     step 02/6 | loss=3.3482
     step 03/6 | loss=3.9749
     step 04/6 | loss=3.9818
     step 05/6 | loss=2.6916
     step 06/6 | loss=2.7310
     Manual SFT smoke loop complete.
     mean_loss: 3.555134932200114
     ```
   - Saved adapter:
     ```text
     /kaggle/working/adapter_smoke_v1_1
     adapter_config.json
     adapter_model.safetensors
     ```
   - Debug zip:
     ```text
     /kaggle/working/adapter_smoke_v1_1.zip
     ```

## Work completed today

### 1. Built Notebook C

Created and pushed:

```text
notebooks/nemotron_adapter_validation_submission_v1_1_20260504.ipynb
```

Purpose:
- discover adapter output from Notebook B
- validate adapter structure
- check LoRA rank <= 32
- create minimal root-level `submission.zip`
- optionally run vLLM metric-like adapter loading/generation validation

Initial Notebook C commit:

```text
7732f8f37342d5286982370fc0e0d22938431bcc
feat: add adapter validation and packaging notebook
```

### 2. Fixed adapter discovery

Initial Notebook C did not find nested Kaggle notebook-output paths. It was patched to scan:

```text
/kaggle/input/notebooks/**/adapter_config.json
/kaggle/input/**/adapter_model.safetensors
/kaggle/input/**/adapter_smoke_v1_1.zip
/kaggle/working/**
```

Relevant commit:

```text
0e0ac1a5d50acdb95def4d7e4006b7240f73d73b
fix: improve Notebook C adapter discovery diagnostics
```

Observed visible adapter files:

```text
/kaggle/input/notebooks/jatalepawan/nemotron-sft-smoke-run-v1-1-trace-corpus-2026/adapter_smoke_v1_1/adapter_config.json
/kaggle/input/notebooks/jatalepawan/nemotron-sft-smoke-run-v1-1-trace-corpus-2026/adapter_smoke_v1_1/adapter_model.safetensors
/kaggle/input/notebooks/jatalepawan/nemotron-sft-smoke-run-v1-1-trace-corpus-2026/adapter_smoke_v1_1.zip
```

### 3. Packaging-only validation passed

Notebook C with:

```python
RUN_VLLM_VALIDATION = False
VALIDATION_N = 3
MANUAL_ADAPTER_DIR = None
```

Selected:

```text
ADAPTER_DIR = /kaggle/working/adapter_for_validation_v1_1
```

Adapter files:

```text
adapter_config.json 0.00 MB
adapter_model.safetensors 3359.18 MB
```

Adapter config passed:

```text
peft_type = LORA
task_type = CAUSAL_LM
r = 32
lora_alpha = 64
lora_dropout = 0.05
target_modules = .*\.(in_proj|out_proj|up_proj|down_proj)$
```

Created final-style zip:

```text
/kaggle/working/submission.zip
size: 2425.52 MB
contents: ['adapter_config.json', 'adapter_model.safetensors']
```

Result:

```text
Minimal submission.zip packaging validation passed.
```

### 4. Fixed vLLM bootstrap command

The first vLLM validation run failed because Notebook C used:

```bash
uv pip uninstall -y torch torchvision torchaudio
```

But Kaggle `uv pip uninstall` does not accept `-y`. Patched to match the official metric:

```bash
uv pip uninstall torch torchvision torchaudio
```

Relevant commit:

```text
25d62918150207bc1a40f1b1f002bba7ba764f25
fix: match metric uv uninstall command
```

### 5. Fixed utility script path detection

The next vLLM attempt failed because this official metric-only path was absent in the normal user notebook session:

```text
/kaggle/usr/lib/notebooks/metric/nvidia_metric_utility_script
```

Patched Notebook C to auto-detect available utility roots, including Ryan Holbrook variants:

```text
/kaggle/usr/lib/notebooks/metric/nvidia_metric_utility_script
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia_utility_script
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia-utility-script
/kaggle/input/nvidia-utility-script
/kaggle/input/notebooks/ryanholbrook/nvidia-utility-script
/kaggle/input/notebooks/ryanholbrook/nvidia_utility_script
```

Relevant commit:

```text
4416a21edf84cf51901c5b06a76855ad26cc7d4a
fix: auto-detect vLLM utility script path
```

Observed success after patch:

```text
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia-utility-script exists=True
Running: uv pip uninstall torch torchvision torchaudio
Running: tar -cf - -C /kaggle/usr/lib/notebooks/ryanholbrook/nvidia-utility-script . | tar -xf - -C /tmp
chmod +x: /tmp/triton/backends/nvidia/bin/ptxas
chmod +x: /tmp/triton/backends/nvidia/bin/ptxas-blackwell
Utility copied to /tmp from: /kaggle/usr/lib/notebooks/ryanholbrook/nvidia-utility-script
```

### 6. vLLM local validation blocked

After utility bootstrap succeeded, local vLLM validation failed with:

```text
ModuleNotFoundError: No module named 'vllm'
```

Interpretation:
- This is not an adapter or packaging issue.
- The normal user Kaggle notebook environment plus Ryan Holbrook utility script does not expose `vllm`.
- The official evaluator has its own metric runtime with vLLM, but this local notebook session does not.

Current validation state:

```text
Packaging validation: PASS
Adapter discovery: PASS
LoRA rank validation: PASS
Minimal submission.zip: PASS
Local vLLM validation: BLOCKED by missing vLLM
```

### 7. Submitted smoke adapter for scoring

A final-style `submission.zip` was submitted to Kaggle as a **validity test**, not as a competitive score attempt.

Important context:
- The adapter is trained on only 6 examples.
- Expected leaderboard score is likely poor.
- The purpose is to confirm Kaggle evaluator accepts and loads the adapter package.

Status:

```text
Score awaited.
```

## Current state at session end

Working:
- Notebook A trace generation v1.1
- Notebook B manual LoRA smoke training
- Notebook C adapter discovery and packaging
- Root-level `submission.zip` creation
- Kaggle submission made for evaluator compatibility test

Blocked / unknown:
- Kaggle score/result pending
- Local vLLM validation blocked due to missing `vllm` in normal notebook session
- Whether evaluator can load the 2.4 GB zip is not yet known
- Whether the 6-example smoke adapter affects score meaningfully is not expected and not the main goal

## Key risks

1. **Adapter is large**
   - `adapter_model.safetensors`: ~3.36 GB
   - `submission.zip`: ~2.43 GB
   - Need to confirm evaluator accepts package size and loads adapter.

2. **Smoke adapter is not competitive**
   - Only 6 examples were used.
   - Submission is for validity testing only.

3. **Local vLLM validation unavailable**
   - Normal notebook lacks `vllm`.
   - Serving validation depends on Kaggle evaluator or a future runtime with vLLM available.

4. **Loss is full-text loss**
   - Notebook B currently trains on full chat text, not assistant-only loss.
   - This is acceptable for smoke mechanics but likely suboptimal for real SFT.

5. **Trace coverage incomplete**
   - Current v1.1 only covers numeral, gravity, unit_conversion.
   - Needed next: bit_manipulation, cipher, equation_numeric.

## Next steps when score returns

### If submission fails to score / evaluator rejects package
1. Inspect Kaggle error.
2. Check if failure is:
   - zip size
   - adapter format
   - target module mismatch
   - vLLM LoRA loading compatibility
   - missing `adapter_config.json`
3. Patch Notebook C or Notebook B accordingly before scaling training.

### If submission scores successfully, regardless of score
1. Treat adapter format as evaluator-compatible.
2. Move to SFT scaling:
   - 6 examples → 64 examples
   - 64 → 128 examples
   - then larger curated v1.1 set
3. Add assistant-only loss masking or use better SFT path.
4. Expand Notebook A categories:
   - bit_manipulation
   - cipher
   - equation_numeric

### If score is unexpectedly decent
1. Preserve this exact adapter and notebook version.
2. Run controlled ablations:
   - sample size
   - target modules
   - assistant-only loss
   - trace categories
   - shorter vs longer traces

## One-line summary
Notebook C now packages a valid-looking root-level LoRA `submission.zip`; local vLLM validation is blocked by missing `vllm`, so the smoke adapter was submitted to Kaggle as a package-validity test and the score is awaited.
