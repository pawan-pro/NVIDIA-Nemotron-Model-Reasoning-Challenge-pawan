# Session Notes — 2026-05-20

## Project
Kaggle — NVIDIA Nemotron Model Reasoning Challenge

## Session objective
Continue Notebook D category validation from the prior sessions, resolve remaining runtime blockers, run the validation/submission notebook on RTX Pro 6000, and submit the generated notebook output for scoring.

## Starting state
- Current best public score before this session: **0.56**.
- Best adapter before this session: `adapter_sft_v2_bit_bal128_asstloss`.
- Best known submission before this session: **Notebook C-Adapter Validation & Submission Pack Tr — Version 15**.
- Keep Version 15 selected until a higher score is confirmed.
- Notebook B CLI: COMPLETE.
- Notebook C CLI: COMPLETE and created valid `submission.zip`.
- Notebook D category-validation workflow existed, but CLI execution was blocked by metric/vLLM utility mounting issues.

## Prior Notebook D blockers summarized
Notebook D had been debugged through several stages:

1. **Missing vLLM**
   - Error: `ModuleNotFoundError: No module named 'vllm'`.
   - Interpretation: Notebook D could find the adapter and train.csv, but the metric/vLLM runtime was missing.

2. **Metric utility not mounted through CLI**
   - Even after adding `metric/nvidia-metric-utility-script` to CLI metadata, Kaggle CLI did not mount the real metric utility path.
   - Diagnostics showed only Ryan Holbrook utility script under `/kaggle/usr/lib/notebooks/ryanholbrook`.
   - Conclusion: Notebook D category validation should be run through Kaggle web UI with utility scripts attached directly.

3. **Model input missing in web UI**
   - vLLM import passed after adding utility scripts, but the Nemotron base model was initially missing.
   - `/kaggle/input` showed only competitions and notebooks, not models.
   - Fix: add Nemotron base model input in Kaggle UI.

4. **Wrong GPU / P100 incompatibility**
   - Running on P100 caused:

```text
Tesla P100-PCIE-16GB with CUDA capability sm_60 is not compatible with the current PyTorch installation.
CUDA error: no kernel image is available for execution on the device
```

   - Interpretation: the metric/vLLM runtime requires a newer CUDA-capable GPU.
   - Required fix: run on **RTX Pro 6000**, not P100.

## 2026-05-20 runtime setup
User started the 2026-05-20 session and ran the notebook on **RTX Pro 6000**.

Confirmed correct utility bootstrap:

```text
Copying utility root to /tmp
chmod +x: /tmp/triton/backends/nvidia/bin/ptxas
chmod +x: /tmp/triton/backends/nvidia/bin/ptxas-blackwell
TRITON_PTXAS_PATH: /tmp/triton/backends/nvidia/bin/ptxas
TRITON_PTXAS_BLACKWELL_PATH: /tmp/triton/backends/nvidia/bin/ptxas-blackwell
/tmp/vllm exists: True
Bootstrap complete
```

This confirmed that the metric/vLLM utility was correctly mounted and copied into `/tmp`.

## Model path issue and fix
After restarting the kernel/session, the helper function `resolve_model_path()` was not defined, causing:

```text
NameError: name 'resolve_model_path' is not defined
```

The notebook input state showed:

```text
/kaggle/input/competitions
/kaggle/input/models
/kaggle/input/notebooks
```

This meant the model input was now correctly attached, but the helper function cell needed to be rerun or redefined.

Recommended robust model resolver:

```python
from pathlib import Path

def resolve_model_path():
    candidates = [
        Path('/kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1'),
        Path('/kaggle/input/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1'),
        Path('/kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default'),
        Path('/kaggle/input/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default'),
    ]
    for p in candidates:
        if p.exists():
            return str(p)
    for pattern in [
        '**/nemotron-3-nano-30b-a3b-bf16/**/transformers/default/1',
        '**/nemotron-3-nano-30b-a3b-bf16/**/transformers/default',
        '**/transformers/default/1',
        '**/transformers/default',
    ]:
        matches = sorted(Path('/kaggle/input').glob(pattern))
        if matches:
            return str(matches[0])
    raise FileNotFoundError('Could not find Nemotron model path under /kaggle/input')
```

Expected resolved model path:

```text
/kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1
```

## vLLM/model loading progress
The notebook then progressed to real vLLM model initialization on RTX Pro 6000.

Key log evidence:

```text
INFO ... Resolved architecture: NemotronHForCausalLM
INFO ... Using max model len 8192
INFO ... Starting to load model /kaggle/input/models/metric/nemotron-3-nano-30b-a3b-bf16/transformers/default/1
Loading safetensors checkpoint shards: 92% Completed | 12/13
```

Interpretation:
- The notebook passed all prior blockers.
- vLLM was now actually loading the 30B base model.
- Remaining runtime was expected to include model load completion, vLLM warmup/compile, LoRA load, generation over the validation/sample set, and CSV writing.

Estimated runtime shared:

```text
30–90 minutes total for the full 950-row validation/submission run
```

or roughly:

```text
20–60 minutes remaining after model shard loading reached 12/13
```

## Notebook run completion
User later reported that the notebook ran successfully.

Evidence from notebook output:

```text
assert (DEBUG_DIR / 'validation_predictions.csv').exists(), 'validation_predictions.csv missing'
assert (DEBUG_DIR / 'category_summary.csv').exists(), 'category_summary.csv missing'
print('Notebook D category validation complete.')

Notebook D category validation complete.
```

This confirms that the validation outputs were created:

```text
/kaggle/working/category_validation/validation_predictions.csv
/kaggle/working/category_validation/category_summary.csv
```

The notebook also displayed at least one qualitative failure case showing an equation-style prompt where the model generated a long unboxed response and `has_boxed: False`. This suggests boxed-answer failures and/or equation-category behavior may be important to inspect in the saved validation CSVs.

## Submission status
User reported that the notebook was submitted for scoring.

Current submission state:
- Notebook run completed.
- Notebook was submitted for scoring.
- Public score was not yet reported in this session note.
- Keep Version 15 / 0.56 selected until the new score is known and higher.

## Current live state at close

| Component | Status |
|---|---|
| Current best before new score | Version 15 / 0.56 |
| Notebook B CLI | COMPLETE |
| Notebook C CLI | COMPLETE + valid `submission.zip` |
| Notebook D CLI | blocked by missing metric/vLLM mount |
| Notebook D web UI | vLLM PASS |
| Notebook D on RTX Pro 6000 | ran successfully |
| Category validation outputs | created |
| New notebook submission | submitted for scoring |
| New public score | pending / not yet reported |

## Immediate next session actions
1. Check Kaggle submission score for the newly submitted notebook.
2. If score is **> 0.56**, select/preserve it as the new best.
3. If score is **<= 0.56**, keep Version 15 selected.
4. Inspect Notebook D validation outputs:
   - `category_summary.csv`
   - `validation_predictions.csv`
   - `boxed_summary.csv` if available
   - `mistakes/<category>.csv` files
5. Identify whether failures are concentrated in:
   - cipher,
   - bit manipulation,
   - equation numeric,
   - cryptarithm,
   - missing `\boxed{}` formatting,
   - or easy-category forgetting.

## Decision matrix after score and category validation

| Result | Meaning | Action |
|---|---|---|
| New score > 0.56 | notebook/submission improved | select new best and document change |
| New score = 0.56 | equivalent to current best | use Notebook D diagnostics for next experiment |
| New score < 0.56 | regression or duplicate/validation artifact | keep Version 15 selected |
| Many `has_boxed=False` failures | formatting issue | improve final-answer formatting in traces |
| Cipher weak | missing high-yield category | add cipher trace builder in Notebook A v3 |
| Bit weak | bit traces insufficient | improve bit-serial traces |
| Easy categories weak | forgetting / imbalance | rebalance training mix |
| Equation weak | missing category coverage | add clean equation_numeric trace subset |

## Recommended next experiment direction
Do not blindly scale examples yet.

Next high-ROI move should be driven by Notebook D outputs:

1. If cipher is weak: build `Notebook A v3` with a cipher trace builder.
2. If bit is weak: improve bit-serial traces before increasing sample size.
3. If boxed-answer format is weak: add stricter answer-format examples and validation.
4. If equation numeric is weak but easy categories are strong: add only the clean deterministic equation subset.

## Guardrails
- Keep Version 15 / 0.56 selected until a better score is confirmed.
- Do not submit from diagnostic notebooks unless they generate a valid `submission.zip` and are intended for leaderboard scoring.
- Do not continue Notebook D via CLI until a reliable metric/vLLM mount solution exists.
- Use Kaggle web UI with NVIDIA Metric Utility Script + NVIDIA Utility Script for vLLM validation.
- Continue using K-12 summaries before Codex meta-prompts.
