# Session Notes — 2026-04-30

## Project
Kaggle — NVIDIA Nemotron Model Reasoning Challenge

## Main outcome
Today moved the project from trace generation only to a working SFT smoke path.

The key achievement is that both Notebook A and Notebook B now run in Kaggle:

1. Notebook A generates a clean v1.1 trace corpus.
2. Notebook B performs a tiny manual LoRA/PEFT smoke update and saves a valid adapter artifact.

This does not yet mean the adapter is competitive or submission-ready. It means the training path is now operational at smoke-test scale.

## Notebook A — Trace Generator v1.1

Notebook:
`notebooks/nemotron_trace_generator_20260429.ipynb`

Purpose:
- read competition `train.csv`
- classify problems into initial supported categories
- generate deterministic boxed-answer SFT traces
- save SFT corpus artifacts

Validated output:

```text
Generated records: 4767
Skipped records: 4733
Validation passed.
All assistant traces contain literal \boxed{...}.
All boxed answers exactly match train.answer.
No backspace escape bug detected.
No inconsistent stated numeric values detected.
```

Artifacts:

```text
/kaggle/working/train_traces_v1_1.jsonl
/kaggle/working/train_traces_v1_1_preview.csv
/kaggle/working/train_traces_v1_1_stats.csv
/kaggle/working/train_traces_v1_1_skipped.csv
```

Category counts:

```text
gravity            1597
unit_conversion    1594
numeral            1576
```

Important fix:
- v1 had correct boxed answers but unit-conversion traces could contain contradictory intermediate arithmetic.
- v1.1 made unit-conversion traces conservative, avoiding incorrect stated computed values while keeping final boxed answer correct.

## Notebook B — SFT Smoke Run v1.1

Notebook:
`notebooks/nemotron_sft_smoke_v1_1_20260430.ipynb`

Purpose:
- consume `train_traces_v1_1.jsonl`
- avoid TRL dependency because Kaggle pinned environment had no `trl` and internet was off
- run a tiny manual PEFT/LoRA training loop
- prove backward pass + optimizer + adapter save

Key runtime dependencies:
- `mamba_ssm` was missing initially.
- Fix was to add the Ryan Holbrook NVIDIA utility script path.
- Working utility root in Kaggle was:

```text
/kaggle/usr/lib/notebooks/ryanholbrook/nvidia_utility_script
```

Successful import:

```text
mamba_ssm import OK: /kaggle/usr/lib/notebooks/ryanholbrook/nvidia_utility_script/mamba_ssm/__init__.py
```

Smoke settings:

```text
SMOKE_SAMPLE_SIZE = 6
MAX_SEQ_LEN = 384
LR = 2e-4
LoRA rank = 32
LoRA alpha = 64
LoRA dropout = 0.05
LoRA targets = .*(in_proj|out_proj|up_proj|down_proj)$
```

Successful smoke run:

```text
step 01/6 | loss=4.6033
step 02/6 | loss=3.3482
step 03/6 | loss=3.9749
step 04/6 | loss=3.9818
step 05/6 | loss=2.6916
step 06/6 | loss=2.7310
Manual SFT smoke loop complete.
mean_loss: 3.555134932200114
```

Adapter save validation:

```text
Smoke adapter save validation passed.
/kaggle/working/adapter_smoke_v1_1
```

Adapter files:

```text
adapter_config.json
adapter_model.safetensors
```

Debug zip created:

```text
/kaggle/working/adapter_smoke_v1_1.zip
```

Zip contents:

```text
adapter_config.json
adapter_model.safetensors
```

Size:

```text
2423.04 MB
```

## Current state

Working:
- trace generation v1.1
- boxed-answer formatting
- conservative trace validation
- Kaggle input mount for trace corpus
- mamba_ssm utility path setup
- model load
- LoRA attachment
- manual backward pass
- optimizer step
- adapter save
- debug adapter zip

Not yet done:
- adapter validation with metric-like inference
- final submission packaging
- leaderboard submission
- full SFT run on all 4,767 examples
- expansion to cipher / bit manipulation / equation_numeric

## Important note for next session
Before changing code or scaling training, we need to go through Notebook A and Notebook B carefully for understanding.

Notebook A topics to understand:
- how categories are classified
- why v1.1 uses conservative unit-conversion traces
- how boxed-answer validation works
- how trace artifacts are created and consumed

Notebook B topics to understand:
- why TRL was removed
- how the manual training loop works
- why the utility path is needed for `mamba_ssm`
- why LoRA targets use `in_proj|out_proj|up_proj|down_proj`
- what the 2.4GB adapter size implies for packaging and Kaggle submission
- why smoke success is not the same as leaderboard readiness

## Next recommended steps

### P0 — Understand Notebook A and B
Read both notebooks line by line before scaling anything.

### P1 — Build Notebook C: adapter validation and packaging
Goal:
- load `/kaggle/working/adapter_smoke_v1_1`
- validate with metric-like inference on a few known training examples
- check generated boxed answer behavior
- create minimal `submission.zip` containing only adapter files

### P2 — Decide scaling path
Only after Notebook C validates serving compatibility:
- increase smoke sample size from 6 to 64 or 128
- monitor memory and runtime
- then consider full 4,767-example SFT

### P3 — Improve data coverage
After training path is stable:
- add cipher traces
- add bit_manipulation traces
- add equation_numeric traces

## Risks / Watchouts

1. Adapter is large: `adapter_model.safetensors` is several GB. Need to confirm final `submission.zip` size and Kaggle upload compatibility.
2. Smoke training used only 6 examples. It proves mechanics, not accuracy.
3. Notebook B currently uses manual loss on the entire chat text. Later we may need assistant-only loss masking for better SFT quality.
4. Unit-conversion traces are conservative and may improve answer-format learning but may not teach real solving rules.
5. Next leaderboard gains likely require category expansion, especially bit manipulation and cipher.

## One-line summary
Today we got a clean v1.1 trace corpus and a successful manual LoRA SFT smoke run that saves adapter artifacts; next session should first review Notebook A and Notebook B for understanding, then build Notebook C for adapter validation and packaging.
